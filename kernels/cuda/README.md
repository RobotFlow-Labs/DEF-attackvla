# CUDA Kernels (DEF-attackvla)

- `patch_guard_kernel.cu`
Purpose:
- Scaffold for fused patch scoring used in defense-time trigger saliency checks.

Notes:
- This is a kernel interface scaffold and requires integration bindings before production use.
